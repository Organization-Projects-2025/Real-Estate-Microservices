{
  "name": "real-estate-selenium-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone Selenium WebDriver tests for the Real Estate Microservices project.",
  "scripts": {
    "test": "mocha \"tests/**/*.test.js\" --timeout 300000",
    "test:headed": "HEADLESS=false mocha \"tests/**/*.test.js\" --timeout 300000"
  },
  "dependencies": {
    "mocha": "^10.8.2",
    "selenium-webdriver": "^4.27.0"
  }
}
